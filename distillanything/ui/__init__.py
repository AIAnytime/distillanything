"""The Distill Anything dashboard: a local-first web UI over runs, datasets, and jobs.

Everything the UI shows comes from files the CLI already writes (status.json,
metrics.jsonl, report.json, ...), so runs started from the terminal appear in
the dashboard too. Requires: pip install "distill-anything[ui]".
"""
